very-so-much-count: 1
very-so-much-1: London English ColdBeer

very-so-much: A1 B1 C1, A B C

server:
  port: ${vcap.application.port:8089}


eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://127.0.0.1:8761}/eureka/

---
spring:
  profiles: cloud
eureka:
  instance:
    hostname: ${APPLICATION_DOMAIN}
    nonSecurePort: 80
