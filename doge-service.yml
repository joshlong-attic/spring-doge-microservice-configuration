very-so-much-count: 1
very-so-much-1: Pivotal Agile Cloudy

server:
  port: ${vcap.application.port:8089}


eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://127.0.0.1:8761}/eureka/
  instance:
    hostname: ${APPLICATION_DOMAIN:http://127.0.0.1:${server.port}}
    nonSecurePort: 80
